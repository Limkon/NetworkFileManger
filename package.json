{
  "name": "network-file-manager",
  "version": "1.0.0",
  "description": "A secure network file manager running on Cloudflare Workers",
  "main": "src/worker.js",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "setup": "node setup.js",
    "deploy": "node setup.js && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "bcryptjs": "^2.4.3",
    "hono": "^3.12.12"
  },
  "devDependencies": {
    "wrangler": "^3.114.15"
  }
}
